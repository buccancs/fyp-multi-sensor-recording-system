<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingStart="20dp"
    android:paddingEnd="20dp"
    android:paddingBottom="16dp"
    tools:background="@color/background_dark">

    <TextView
        android:id="@+id/tv_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:paddingTop="17dp"
        android:paddingBottom="12dp"
        android:textSize="16sp"
        android:textColor="@color/white"
        tools:text="Multi-Sensor Devices"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <View
        android:id="@+id/iv_bg"
        android:layout_width="0dp"
        android:layout_height="80dp"
        android:background="@drawable/device_card_bg"
        android:elevation="2dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/tv_title" />

    <!-- Device icon -->
    <TextView
        android:id="@+id/tv_device_icon"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_marginStart="24dp"
        android:gravity="center"
        android:textSize="24sp"
        android:text="📱"
        android:background="@drawable/button_secondary_enhanced"
        app:layout_constraintStart_toStartOf="@id/iv_bg"
        app:layout_constraintTop_toTopOf="@id/iv_bg"
        app:layout_constraintBottom_toBottomOf="@id/iv_bg" />

    <TextView
        android:id="@+id/tv_device_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="20dp"
        android:layout_marginEnd="16dp"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textColor="@color/text_primary"
        android:maxLines="1"
        android:ellipsize="end"
        tools:text="RGB Camera"
        app:layout_constraintStart_toEndOf="@id/tv_device_icon"
        app:layout_constraintEnd_toStartOf="@id/view_device_state"
        app:layout_constraintTop_toTopOf="@id/iv_bg"
        app:layout_constraintBottom_toTopOf="@+id/tv_device_status" />

    <!-- Enhanced status indicator with professional styling -->
    <View
        android:id="@+id/view_device_state"
        android:layout_width="12dp"
        android:layout_height="12dp"
        android:layout_marginEnd="24dp"
        android:background="@drawable/status_dot_offline"
        app:layout_constraintEnd_toEndOf="@id/iv_bg"
        app:layout_constraintTop_toTopOf="@+id/tv_device_name"
        app:layout_constraintBottom_toBottomOf="@+id/tv_device_name" />
        
    <TextView
        android:id="@+id/tv_device_status"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:textSize="14sp"
        android:textColor="@color/text_secondary"
        android:alpha="0.8"
        tools:text="Ready for connection"
        app:layout_constraintStart_toStartOf="@id/tv_device_name"
        app:layout_constraintEnd_toStartOf="@id/view_device_state"
        app:layout_constraintTop_toBottomOf="@id/tv_device_name"
        app:layout_constraintBottom_toBottomOf="@id/iv_bg" />
        
    <TextView
        android:id="@+id/tv_device_state"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="8dp"
        android:textSize="12sp"
        android:textStyle="bold"
        android:textColor="@color/status_offline"
        tools:text="offline"
        app:layout_constraintEnd_toStartOf="@id/view_device_state"
        app:layout_constraintTop_toTopOf="@id/view_device_state"
        app:layout_constraintBottom_toBottomOf="@id/view_device_state" />

</androidx.constraintlayout.widget.ConstraintLayout>